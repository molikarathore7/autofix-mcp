1. Identify the exact bug.
The `get_email` endpoint attempts to access the customer's email using the key "email_address" which does not exist, instead of the correct key "email".

2. Explain why it occurred in short one line.
The code tries to retrieve a dictionary value using an incorrect key, "email_address", causing a KeyError.

3. Suggest the fix in short one line.
Change `customer["email_address"]` to `customer["email"]` in the `get_email` function.

4. Return the corrected code snippet.
@app.get("/customer-email")
def get_email():

    try:

        customer = customers[0]

        # Intentional Bug
        return customer["email"]

    except Exception as e:

        logging.error(f"Customer Email Error: {str(e)}")

        return {
            "error": str(e)
        }