1. Identify the exact bug.
The /customer-email endpoint attempts to access the customer's email using the dictionary key "email_address", but the customer data is stored with the key "email".

2. Explain why it occurred in short one line.
The code tries to retrieve a dictionary value using a non-existent key "email_address" instead of the correct key "email".

3. Suggest the fix in short one line.
Change the dictionary key access from "email_address" to "email" within the get_email function.

4. Return the corrected code snippet.
@app.get("/customer-email")
def get_email():

    try:

        customer = customers[0]

        # Intentional Bug
        return customer["email"]

    except Exception as e:

        logging.error(f"Customer Email Error: {str(e)}")

        return {
            "error": str(e)
        }