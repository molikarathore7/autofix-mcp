1. Identify the exact bug.
The /customer-email endpoint attempts to access a customer's email using the key "email_address" which does not exist in the customer dictionary; the correct key is "email".

2. Explain why it occurred in short one line.
The code uses an incorrect dictionary key to retrieve the email address.

3. Suggest the fix in short one line.
Change the dictionary key from "email_address" to "email" in the /customer-email endpoint.

4. Return the corrected code snippet.
@app.get("/customer-email")
def get_email():

    try:

        customer = customers[0]

        # Fix: Use the correct key 'email'
        return customer["email"]

    except Exception as e:

        logging.error(f"Customer Email Error: {str(e)}")

        return {
            "error": str(e)
        }