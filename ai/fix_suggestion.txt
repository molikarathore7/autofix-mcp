1. The exact bug is a KeyError caused by attempting to access a customer's email using the key 'email_address' when the data is stored with the key 'email'.
2. A KeyError occurs because the dictionary key 'email_address' does not exist; the correct key is 'email'.
3. Change customer["email_address"] to customer["email"] in the get_email endpoint.
4.
@app.get("/customer-email")
def get_email():

    try:

        customer = customers[0]

        # Intentional Bug
        return customer["email"]

    except Exception as e:

        logging.error(f"Customer Email Error: {str(e)}")

        return {
            "error": str(e)
        }